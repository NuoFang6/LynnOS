diff --git a/include/target.mk b/include/target.mk
index 9939658ea4..9271e54040 100644
--- a/include/target.mk
+++ b/include/target.mk
@@ -280,9 +280,10 @@ ifeq ($(DUMP),1)
     CPU_CFLAGS_ultrasparc = -mcpu=ultrasparc
   endif
   ifeq ($(ARCH),aarch64)
-    CPU_TYPE ?= generic
-    CPU_CFLAGS_generic = -mcpu=generic
-    CPU_CFLAGS_cortex-a53 = -mcpu=cortex-a53
+    CPU_TYPE = cortex-a53
+    CPU_CFLAGS = -O2 -pipe -fpredictive-commoning -ftree-partial-pre -fgcse-after-reload -floop-interchange -fschedule-insns -fsched-pressure -ftree-vectorize -fvect-cost-model=cheap -funsafe-math-optimizations -fno-math-errno -fdevirtualize-at-ltrans -fipa-pta -fno-plt -fno-semantic-interposition -mno-outline-atomics -fno-unwind-tables -fno-asynchronous-unwind-tables -fweb -frename-registers
+    CPU_CFLAGS_generic = -mcpu=cortex-a53+crypto+crc
+    CPU_CFLAGS_cortex-a53 = -mcpu=cortex-a53+crypto+crc
   endif
   ifeq ($(ARCH),arc)
     CPU_TYPE ?= arc700
