diff --git a/include/target.mk b/include/target.mk
index 9939658ea4..9271e54040 100644
--- a/include/target.mk
+++ b/include/target.mk
@@ -280,9 +280,9 @@ ifeq ($(DUMP),1)
     CPU_CFLAGS_ultrasparc = -mcpu=ultrasparc
   endif
   ifeq ($(ARCH),aarch64)
-    CPU_TYPE ?= generic
-    CPU_CFLAGS_generic = -mcpu=generic
-    CPU_CFLAGS_cortex-a53 = -mcpu=cortex-a53
+    CPU_TYPE = cortex-a53
+    CPU_CFLAGS_generic = -fgcse-after-reload -floop-interchange -fdevirtualize-at-ltrans -fweb -frename-registers -fno-plt -mcpu=cortex-a53+crypto+crc
+    CPU_CFLAGS_cortex-a53 = -fgcse-after-reload -floop-interchange -fdevirtualize-at-ltrans -fweb -frename-registers -fno-plt -mcpu=cortex-a53+crypto+crc
   endif
   ifeq ($(ARCH),arc)
     CPU_TYPE ?= arc700
