--- a/include/image.mk
+++ b/include/image.mk
@@ -88,5 +88,8 @@
   endif
   SQUASHFSCOMP := xz $(LZMA_XZ_OPTIONS) $(BCJ_FILTER)
 endif
+ifeq ($(CONFIG_SQUASHFS_ZSTD),y)
+  SQUASHFSCOMP := zstd -Xcompression-level 17
+endif
 
 JFFS2_BLOCKSIZE ?= 64k 128k
